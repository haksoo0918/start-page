# PRD: 데스크톱 브라우저 시작 페이지 (Desktop Browser Start Page)

## 1. 개요 및 목표
데스크톱 브라우저를 켰을 때 **자주 가는 링크(60% 메인)** 를 중심으로, **일산 등 시/군/구 상세 지역의 오늘/내일 날씨 및 강수확률** 과 **7대 핵심 시장 시세(KOSPI, KOSDAQ, S&P 500, NASDAQ, TLT, GOLD, BTC)** 를 한눈에 볼 수 있는 깔끔하고 정갈한 개인화 대시보드를 구축합니다.

---

## 2. 신규 기능 요구사항: 바로가기 URL 우선 입력 및 웹페이지 제목 자동 가져오기

### 2.1 모달 필드 순서 변경
- `[1. 웹사이트 주소(URL)]` 입력칸을 맨 위에 배치하고 자동 포커스(`autoFocus`).
- `[2. 바로가기 이름]` 입력칸을 그 아래에 배치.

### 2.2 웹페이지 제목 자동 완성
- 주소(URL)를 입력하면 해당 웹페이지의 제목(HTML `<title>`)을 가져와 제목 입력칸에 자동으로 채워 넣음.
- 사용자가 원하는 경우 제목을 직접 수정 가능.

### 2.3 TDD 검증 계획
- `src/utils/urlHelper.test.ts`: URL 정규화 및 HTML title 태그 파싱 단위 테스트 작성.

---

## 3. 진행 절차
1. **PRD 및 계획서 업데이트** (현재 단계)
2. **사용자 승인 확인** 
3. **TDD 테스트 작성 및 제목 파싱 로직 구현** 
4. **BookmarkModal.tsx 필드 순서 변경 및 제목 자동 채우기 연동** 
5. **테스트/빌드 검증 및 화면 확인** 
